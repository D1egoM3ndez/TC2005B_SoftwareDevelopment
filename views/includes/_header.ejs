<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokédex</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/pokedex.css">
</head>
<body>

    <!-- NAVBAR -->
    <nav class="navbar">
        <div class="navbar-inner">
            <a class="navbar-logo" href="/">Pokédex</a>

            <div class="navbar-links">
                <a class="navbar-item" href="/">Inicio</a>
                <a class="navbar-item" href="/new">Agregar</a>
            </div>

            <div class="navbar-auth">
                <% if (username == '') { %> 
                    <a class="nav-btn login"  href="/user/login">Log in</a>
                    <a class="nav-btn login"  href="/user/signup">Sign up</a>
                <% } else { %> 
                    <a class="nav-btn logout" href="/user/logout">Log out</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1>Pokédex</h1>